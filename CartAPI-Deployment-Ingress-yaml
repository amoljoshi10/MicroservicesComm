apiVersion: v1
kind: Service
metadata:
  name: cartapi
spec:
  type: ClusterIP
  selector:
    app: cartapi
  ports:
  - port: 80
    name: http-port
    protocol: TCP
    targetPort: 4000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cartapi
spec:
  selector:
    matchLabels:
      app: cartapi
  template:
    metadata:
      labels:
        app: cartapi
    spec:
      containers:
      - name: cartimage
        image: MicroservicesIngressACR.azurecr.io/cartapi:#{Build.BuildId}#
        ports:
          - containerPort: 80
